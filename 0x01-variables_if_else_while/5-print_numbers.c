#include <stdio.h>

/**
 * main - this is the main function
 *
 * Return: this is always 0
 */

int main(void)
{
	char a;

	a = 0;
	while (a < 10)
	{
	printf("%d", a);
	a++;
	}
	printf("\n");
	return (0);
}
