#include "main.h"

/**
 * add - this function adds two numbers
 * @a: this is the first operand
 * @b: this is the second operand
 * Return: this returns the addition of the numbers
 */
int add(int a, int b)
{
	return (a + b);
}
